const reveals=document.querySelectorAll('.reveal-section');const revealOnScroll=()=>{const trigger=window.innerHeight*.88;reveals.forEach(el=>{const top=el.getBoundingClientRect().top;if(top<trigger){el.classList.add('visible')}})};window.addEventListener('scroll',revealOnScroll);revealOnScroll();document.querySelectorAll('.counter').forEach(counter=>{const target=+counter.dataset.target;let current=0;const step=Math.ceil(target/80);const update=()=>{current+=step;if(current>=target){counter.textContent=target}else{counter.textContent=current;requestAnimationFrame(update)}};update()});const mapData={Downtown:'A stronger downtown needs safety, housing, cleanliness, business confidence, and visible leadership.',Byron:'Byron residents care about community character, infrastructure, and responsible growth.',Argyle:'Argyle deserves better traffic flow, safer roads, and investment in everyday city services.',"White Oaks":'White Oaks families want reliable transit, safer neighbourhoods, and better city responsiveness.',Wortley:'Wortley Village reflects the local identity and community spirit Londoners are proud of.'};const mapTitle=document.getElementById('mapTitle');const mapText=document.getElementById('mapText');document.querySelectorAll('.map-pin').forEach(pin=>{pin.addEventListener('click',()=>{document.querySelectorAll('.map-pin').forEach(p=>p.classList.remove('active'));pin.classList.add('active');const area=pin.dataset.area;mapTitle.textContent=area;mapText.textContent=mapData[area]})});const events=[{title:'Downtown Meet & Greet',date:'June 4 · Covent Garden Market'},{title:'Byron Community Conversation',date:'June 8 · Byron Library'},{title:'Housing Roundtable',date:'June 12 · Innovation Works'}];const eventList=document.getElementById('eventList');events.forEach(event=>{const item=document.createElement('article');item.className='event-item';item.innerHTML=`<div><strong>${event.title}</strong><span>${event.date}</span></div><a class="button button-dark" href="#volunteer">Attend</a>`;eventList.appendChild(item)});window.addEventListener('scroll',()=>{document.querySelectorAll('.parallax').forEach(layer=>{const speed=layer.dataset.speed;layer.style.transform=`translateY(${window.scrollY*speed}px)`})});